persona
  v2.0
    ✔ Change `single` and `multi` to take in render specs

    improvements:
      ✔ Call function name should follow the string, not the key name.
      ✔ Value property name should follow the string, not the key name.
      ☐ `single` and `multi` should output the bindings.


